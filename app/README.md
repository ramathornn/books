# Books — app

The Next.js application lives here. See the [top-level README](../README.md) for the full setup guide.

## Quick start

```bash
npm install
cp .env.example .env          # then edit values
npx prisma generate
npx prisma db push
npm run dev
```

Open <http://localhost:3000>.
